services:
  agent:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "${AGENT_OS_PORT:-7777}:7777"
    environment:
      AGENT_DB_FILE: /data/agui_agentos.db
    volumes:
      - agent_data:/data

volumes:
  agent_data:
